import React from 'react'
import {View} from 'react-native'
import PropTypes from 'prop-types'
import Row from './row'

const Grid = (props) => {
  return (
    <View></View>
  )
}

export default Grid